# raw radar captures -- 3.8GB, not source, regenerated by recording
data/

# separate project with its own git history + remote -- not nested here
mmResp/

# python environment / installer -- not source, reinstall instead of tracking
vitals-env/
Miniforge3-MacOSX-arm64.sh
__pycache__/
*.pyc

# training logs -- regenerated by train.py, not useful without tensorboard itself
tb/

# OS cruft
.DS_Store

# local-only Claude Code settings (per-machine, not shared config)
.claude/settings.local.json
